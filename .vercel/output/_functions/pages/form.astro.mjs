import { e as createComponent, r as renderTemplate, n as renderHead, h as createAstro } from '../chunks/astro/server_DeoLMx00.mjs';
import 'piccolore';
import 'clsx';
/* empty css                                */
export { renderers } from '../renderers.mjs';

var __freeze = Object.freeze;
var __defProp = Object.defineProperty;
var __template = (cooked, raw) => __freeze(__defProp(cooked, "raw", { value: __freeze(raw || cooked.slice()) }));
var _a;
const $$Astro = createAstro();
const $$Form = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro, $$props, $$slots);
  Astro2.self = $$Form;
  return renderTemplate(_a || (_a = __template(['<html lang="en" data-astro-cid-xb3inwvi> <head><meta charset="UTF-8"><meta name="viewport" content="width=device-width, initial-scale=1.0"><title>Form - Broker Lenders </title><link rel="icon" type="image/svg+xml" href="/logo.png">', `</head> <body data-astro-cid-xb3inwvi> <div class="container" data-astro-cid-xb3inwvi> <div class="header" data-astro-cid-xb3inwvi> <div class="controls" data-astro-cid-xb3inwvi> <button class="language-toggle" id="languageToggle" aria-label="Cambiar idioma" data-astro-cid-xb3inwvi>
EN
</button> <button class="dark-mode-toggle" id="darkModeToggle" aria-label="Alternar modo oscuro" data-astro-cid-xb3inwvi> <svg class="theme-icon sun-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" data-astro-cid-xb3inwvi> <circle cx="12" cy="12" r="5" data-astro-cid-xb3inwvi></circle> <path d="M12 1v2M12 21v2M4.22 4.22l1.42 1.42M18.36 18.36l1.42 1.42M1 12h2M21 12h2M4.22 19.78l1.42-1.42M18.36 5.64l1.42-1.42" data-astro-cid-xb3inwvi></path> </svg> <svg class="theme-icon moon-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" style="display: none;" data-astro-cid-xb3inwvi> <path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z" data-astro-cid-xb3inwvi></path> </svg> </button> </div> </div> <div class="form-container" data-astro-cid-xb3inwvi> <div class="progress-container" data-astro-cid-xb3inwvi> <div class="progress-bar" data-astro-cid-xb3inwvi> <div class="progress-fill" id="progressFill" data-astro-cid-xb3inwvi></div> </div> <div class="progress-text" id="progressText" data-astro-cid-xb3inwvi>Paso 1 de 2</div> </div> <img src="/logo.png" alt="Logo" class="logo" data-astro-cid-xb3inwvi> <!-- Step 1: Contact Information --> <div class="step active" id="step1" data-astro-cid-xb3inwvi> <h2 class="step-title" data-es="Informaci\xF3n de Contacto" data-en="Contact Information" data-astro-cid-xb3inwvi>Informaci\xF3n de Contacto</h2> <p class="step-description" data-es="Por favor proporciona tus datos de contacto para que podamos comunicarnos contigo." data-en="Please provide your contact details so we can get in touch with you." data-astro-cid-xb3inwvi>Por favor proporciona tus datos de contacto para que podamos comunicarnos contigo.</p> <form id="contactForm" action="/send-email.php" method="POST" data-send-email-url="https://brokerlenders.com/send-email.php" data-astro-cid-xb3inwvi> <div class="form-group" data-astro-cid-xb3inwvi> <label class="form-label" for="fullName" data-es="Nombre Completo *" data-en="Full Name *" data-astro-cid-xb3inwvi>Nombre Completo *</label> <input type="text" id="fullName" name="name" class="form-input" required data-astro-cid-xb3inwvi> <div class="error-message" id="nameError" data-es="Por favor ingresa tu nombre completo" data-en="Please enter your full name" data-astro-cid-xb3inwvi>Por favor ingresa tu nombre completo</div> </div> <div class="form-group" data-astro-cid-xb3inwvi> <label class="form-label" for="phone" data-es="N\xFAmero de Tel\xE9fono *" data-en="Phone Number *" data-astro-cid-xb3inwvi>N\xFAmero de Tel\xE9fono *</label> <input type="tel" id="phone" name="phone" class="form-input" placeholder="(555) 123-4567" required data-astro-cid-xb3inwvi> <div class="error-message" id="phoneError" data-es="Por favor ingresa un n\xFAmero de tel\xE9fono v\xE1lido" data-en="Please enter a valid phone number" data-astro-cid-xb3inwvi>Por favor ingresa un n\xFAmero de tel\xE9fono v\xE1lido</div> </div> <!-- Data Protection Notice --> <div class="data-protection-notice" data-astro-cid-xb3inwvi> <div class="protection-icon" data-astro-cid-xb3inwvi> <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" data-astro-cid-xb3inwvi> <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z" data-astro-cid-xb3inwvi></path> <path d="M9 12l2 2 4-4" data-astro-cid-xb3inwvi></path> </svg> </div> <p class="protection-text" data-es="\u{1F512} Tus datos est\xE1n protegidos y seguros. Utilizamos encriptaci\xF3n SSL y no compartimos tu informaci\xF3n con terceros." data-en="\u{1F512} Your data is protected and secure. We use SSL encryption and do not share your information with third parties." data-astro-cid-xb3inwvi>\u{1F512} Tus datos est\xE1n protegidos y seguros. Utilizamos encriptaci\xF3n SSL y no compartimos tu informaci\xF3n con terceros.</p> </div> <div class="consent-group" data-astro-cid-xb3inwvi> <label class="consent-item" data-astro-cid-xb3inwvi> <input id="privacyConsent" class="consent-input" type="checkbox" required data-astro-cid-xb3inwvi> <span class="consent-label" data-es="Acepto los" data-en="I accept the" data-astro-cid-xb3inwvi>Acepto los</span> <button class="consent-link" type="button" data-modal-open="termsModal" data-es="T\xE9rminos y Condiciones" data-en="Terms & Conditions" data-astro-cid-xb3inwvi>T\xE9rminos y Condiciones</button> <span class="consent-label" data-es="y la" data-en="and the" data-astro-cid-xb3inwvi>y la</span> <button class="consent-link" type="button" data-modal-open="privacyModal" data-es="Pol\xEDtica de Privacidad" data-en="Privacy Policy" data-astro-cid-xb3inwvi>Pol\xEDtica de Privacidad</button> </label> <label class="consent-item" data-astro-cid-xb3inwvi> <input id="updatesConsent" name="updatesConsent" class="consent-input" type="checkbox" checked data-astro-cid-xb3inwvi> <span class="consent-label" data-es="Quiero recibir novedades y actualizaciones por correo." data-en="I want to receive news and updates by email." data-astro-cid-xb3inwvi>Quiero recibir novedades y actualizaciones por correo.</span> </label> <div class="error-message" id="consentError" data-es="Debes aceptar los t\xE9rminos y la pol\xEDtica para continuar." data-en="You must accept the terms and privacy policy to continue." data-astro-cid-xb3inwvi>Debes aceptar los t\xE9rminos y la pol\xEDtica para continuar.</div> </div> <input type="hidden" id="languageInput" name="language" value="es" data-astro-cid-xb3inwvi> <button type="submit" class="btn btn-primary" id="submitBtn" data-astro-cid-xb3inwvi> <div class="loading-spinner" data-astro-cid-xb3inwvi></div> <span class="btn-text" data-es="Enviar" data-en="Submit" data-astro-cid-xb3inwvi>Enviar</span> </button> <!-- Success message after submit --> <div id="submitSuccess" class="success-message" aria-live="polite" data-astro-cid-xb3inwvi> <svg class="success-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" data-astro-cid-xb3inwvi> <polyline class="success-check" points="20,6 9,17 4,12" data-astro-cid-xb3inwvi></polyline> </svg> <span id="submitSuccessText" data-es="Enviado correctamente" data-en="Submitted successfully" data-astro-cid-xb3inwvi>Enviado correctamente</span> </div> <div id="submitError" class="submit-error" role="alert" aria-live="polite" data-astro-cid-xb3inwvi> <svg class="error-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" data-astro-cid-xb3inwvi> <circle cx="12" cy="12" r="9" data-astro-cid-xb3inwvi></circle> <path d="M12 7v6" data-astro-cid-xb3inwvi></path> <path d="M12 17h.01" data-astro-cid-xb3inwvi></path> </svg> <span id="submitErrorText" data-astro-cid-xb3inwvi></span> </div> </form> </div> <!-- Step 2: Thank You --> <div class="step" id="step2" data-astro-cid-xb3inwvi> <div class="thank-you" data-astro-cid-xb3inwvi> <div class="thank-you-icon" data-astro-cid-xb3inwvi> <svg class="checkmark" viewBox="0 0 24 24" data-astro-cid-xb3inwvi> <polyline points="20,6 9,17 4,12" data-astro-cid-xb3inwvi></polyline> </svg> </div> <h2 class="thank-you-title" data-es="\xA1Gracias!" data-en="Thank You!" data-astro-cid-xb3inwvi>\xA1Gracias!</h2> <p class="thank-you-message" data-es="Tu informaci\xF3n ha sido enviada exitosamente. Nos pondremos en contacto contigo pronto." data-en="Your information has been successfully submitted. We'll be in touch with you soon." data-astro-cid-xb3inwvi>Tu informaci\xF3n ha sido enviada exitosamente. Nos pondremos en contacto contigo pronto.</p> <div class="contact-info" data-astro-cid-xb3inwvi> <h3 data-es="Tu Informaci\xF3n" data-en="Your Information" data-astro-cid-xb3inwvi>Tu Informaci\xF3n</h3> <div class="contact-detail" data-astro-cid-xb3inwvi> <span class="contact-label" data-es="Nombre:" data-en="Name:" data-astro-cid-xb3inwvi>Nombre:</span> <span class="contact-value" id="submittedName" data-astro-cid-xb3inwvi>-</span> </div> <div class="contact-detail" data-astro-cid-xb3inwvi> <span class="contact-label" data-es="Tel\xE9fono:" data-en="Phone:" data-astro-cid-xb3inwvi>Tel\xE9fono:</span> <span class="contact-value" id="submittedPhone" data-astro-cid-xb3inwvi>-</span> </div> </div> </div> </div> </div> </div> <div id="privacyModal" class="modal" aria-hidden="true" data-astro-cid-xb3inwvi> <div class="modal-content" role="dialog" aria-modal="true" aria-labelledby="privacyTitle" data-astro-cid-xb3inwvi> <div class="modal-header" data-astro-cid-xb3inwvi> <h3 id="privacyTitle" class="modal-title" data-es="Pol\xEDtica de Privacidad" data-en="Privacy Policy" data-astro-cid-xb3inwvi>Pol\xEDtica de Privacidad</h3> <button class="modal-close" type="button" data-modal-close aria-label="Close" data-astro-cid-xb3inwvi>
\u2715
</button> </div> <div class="modal-body" data-astro-cid-xb3inwvi> <p data-es="Recopilamos tu nombre y tel\xE9fono para contactarte con informaci\xF3n sobre nuestros servicios. No compartimos tus datos con terceros sin tu consentimiento." data-en="We collect your name and phone number to contact you about our services. We do not share your data with third parties without your consent." data-astro-cid-xb3inwvi>Recopilamos tu nombre y tel\xE9fono para contactarte con informaci\xF3n sobre nuestros servicios. No compartimos tus datos con terceros sin tu consentimiento.</p> <p data-es="Puedes solicitar acceso, actualizaci\xF3n o eliminaci\xF3n de tus datos escribiendo a soporte." data-en="You can request access, updates, or deletion of your data by contacting support." data-astro-cid-xb3inwvi>Puedes solicitar acceso, actualizaci\xF3n o eliminaci\xF3n de tus datos escribiendo a soporte.</p> </div> </div> </div> <div id="termsModal" class="modal" aria-hidden="true" data-astro-cid-xb3inwvi> <div class="modal-content" role="dialog" aria-modal="true" aria-labelledby="termsTitle" data-astro-cid-xb3inwvi> <div class="modal-header" data-astro-cid-xb3inwvi> <h3 id="termsTitle" class="modal-title" data-es="T\xE9rminos y Condiciones" data-en="Terms & Conditions" data-astro-cid-xb3inwvi>T\xE9rminos y Condiciones</h3> <button class="modal-close" type="button" data-modal-close aria-label="Close" data-astro-cid-xb3inwvi>
\u2715
</button> </div> <div class="modal-body" data-astro-cid-xb3inwvi> <p data-es="La informaci\xF3n enviada ser\xE1 utilizada \xFAnicamente para fines de contacto y seguimiento sobre los servicios de Broker Lenders." data-en="The information submitted will be used only to contact you regarding Broker Lenders services." data-astro-cid-xb3inwvi>La informaci\xF3n enviada ser\xE1 utilizada \xFAnicamente para fines de contacto y seguimiento sobre los servicios de Broker Lenders   .</p> <p data-es="El env\xEDo del formulario no constituye una relaci\xF3n contractual hasta que exista una propuesta aceptada." data-en="Submitting this form does not create a contractual relationship until a proposal is accepted." data-astro-cid-xb3inwvi>El env\xEDo del formulario no constituye una relaci\xF3n contractual hasta que exista una propuesta aceptada.</p> </div> </div> </div> <script>
        // Global variables
        let currentStep = 1;
        let personalInfo = {
            name: '',
            phone: ''
        };
        let currentLanguage = 'es'; // Default to Spanish
        let sendEmailUrl = '/send-email.php';

        // Initialize when DOM is loaded
        document.addEventListener('DOMContentLoaded', function() {
            initializeTheme(); // Changed from initializeDarkMode to default to light
            initializeLanguage();
            setupFormValidation();
            showStep(1);
            initializeModals();
            
            // Setup event listeners
            const contactForm = document.getElementById('contactForm');
            if (contactForm?.dataset?.sendEmailUrl) {
                sendEmailUrl = contactForm.dataset.sendEmailUrl;
            }
            contactForm.addEventListener('submit', handleFormSubmit);
            document.getElementById('phone').addEventListener('input', formatPhoneNumber);
            document.querySelector('.dark-mode-toggle').addEventListener('click', toggleDarkMode);
            document.querySelector('.language-toggle').addEventListener('click', toggleLanguage);
        });

        // Handle form submission
        async function handleFormSubmit(e) {
            e.preventDefault();
            
            const name = document.getElementById('fullName').value.trim();
            const phone = document.getElementById('phone').value.trim();
            const consent = document.getElementById('privacyConsent').checked;
            const updates = document.getElementById('updatesConsent').checked;
            clearSubmitError();
            
            // Validate inputs
            if (!validateForm(name, phone, consent)) {
                return;
            }

            // Show loading state
            const submitBtn = document.getElementById('submitBtn');
            submitBtn.classList.add('loading');
            submitBtn.disabled = true;
            let successCompleted = false;
            
            try {
                const response = await fetch(\`\${sendEmailUrl}?t=\${Date.now()}\`, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    cache: 'no-store',
                    body: JSON.stringify({ name, phone, updatesConsent: updates, language: currentLanguage })
                });

                const responseText = await response.text();
                let responseData = {};
                try { responseData = JSON.parse(responseText); } catch {}
                const responseSuccess = response.ok && (responseData.success !== false);
                if (responseSuccess) {
                    // Store submitted data
                    personalInfo.name = name;
                    personalInfo.phone = phone;
                    
                    // Show success UI and keep button disabled/hidden
                    showSuccessUI();
                    successCompleted = true;
                    
                    // Transition after a short delay to thank-you step
                    setTimeout(() => {
                        showStep(2);
                    }, 1200);
                } else {
                    const isHtmlResponse = typeof responseText === 'string' && /<!doctype html|<html/i.test(responseText);
                    const apiMessages = {
                        es: isHtmlResponse
                            ? 'El servidor devolvi\xF3 HTML. Esto indica que send-email.php no se est\xE1 ejecutando como PHP.'
                            : 'No se pudo enviar en este momento. Revisa el servicio de correo.',
                        en: isHtmlResponse
                            ? 'The server returned HTML. This means send-email.php is not running as PHP.'
                            : 'We could not submit right now. Please check the email service.'
                    };
                    const detailText = responseData && (responseData.detail || responseData.message)
                        ? \` \${responseData.detail || responseData.message}\`
                        : responseText ? \` \${responseText.slice(0, 200)}\` : '';
                    showSubmitError(\`\${apiMessages[currentLanguage]}\${detailText}\`);
                }
            } catch (error) {
                console.error('Error submitting form:', error);
                const errorMessages = {
                    es: 'No se pudo conectar con el servicio de env\xEDo. Revisa tu conexi\xF3n.',
                    en: 'Could not reach the delivery service. Check your connection.'
                };
                showSubmitError(errorMessages[currentLanguage]);
            } finally {
                // Remove loading state only if not successful
                if (!successCompleted) {
                    submitBtn.classList.remove('loading');
                    submitBtn.disabled = false;
                }
            }
        }

        function showSuccessUI() {
            const submitBtn = document.getElementById('submitBtn');
            const successDiv = document.getElementById('submitSuccess');
            const successText = document.getElementById('submitSuccessText');
            clearSubmitError();
            const messages = { es: 'Enviado correctamente', en: 'Submitted successfully' };
            successText.textContent = messages[currentLanguage];
            submitBtn.classList.remove('loading');
            submitBtn.disabled = true;
            submitBtn.style.display = 'none';
            successDiv.classList.add('show');
        }

        function showSubmitError(message) {
            const submitError = document.getElementById('submitError');
            const submitErrorText = document.getElementById('submitErrorText');
            submitErrorText.textContent = message;
            submitError.classList.add('show');
        }

        function clearSubmitError() {
            const submitError = document.getElementById('submitError');
            const submitErrorText = document.getElementById('submitErrorText');
            submitErrorText.textContent = '';
            submitError.classList.remove('show');
        }

        // Validate form inputs
        function validateForm(name, phone, consent) {
            let isValid = true;
            
            const messages = {
                es: {
                    nameError: 'Por favor ingresa tu nombre completo',
                    phoneError: 'Por favor ingresa un n\xFAmero de tel\xE9fono v\xE1lido',
                    consentError: 'Debes aceptar los t\xE9rminos y la pol\xEDtica para continuar.'
                },
                en: {
                    nameError: 'Please enter your full name',
                    phoneError: 'Please enter a valid phone number',
                    consentError: 'You must accept the terms and privacy policy to continue.'
                }
            };
            
            // Validate name
            if (!name || name.length < 2) {
                showError('nameError', messages[currentLanguage].nameError);
                document.getElementById('fullName').classList.add('error');
                isValid = false;
            } else {
                hideError('nameError');
                document.getElementById('fullName').classList.remove('error');
            }
            
            // Validate phone
            const phoneRegex = /^\\(?([0-9]{3})\\)?[-. ]?([0-9]{3})[-. ]?([0-9]{4})$/;
            if (!phone || !phoneRegex.test(phone.replace(/\\D/g, ''))) {
                showError('phoneError', messages[currentLanguage].phoneError);
                document.getElementById('phone').classList.add('error');
                isValid = false;
            } else {
                hideError('phoneError');
                document.getElementById('phone').classList.remove('error');
            }

            if (!consent) {
                showError('consentError', messages[currentLanguage].consentError);
                isValid = false;
            } else {
                hideError('consentError');
            }
            
            return isValid;
        }

        // Show error message
        function showError(elementId, message) {
            const errorElement = document.getElementById(elementId);
            errorElement.textContent = message;
            errorElement.classList.add('show');
        }

        // Hide error message
        function hideError(elementId) {
            const errorElement = document.getElementById(elementId);
            errorElement.classList.remove('show');
        }

        // Format phone number as user types
        function formatPhoneNumber(e) {
            const input = e.target;
            const value = input.value.replace(/\\D/g, '');
            let formattedValue = '';
            
            if (value.length > 0) {
                if (value.length <= 3) {
                    formattedValue = \`(\${value}\`;
                } else if (value.length <= 6) {
                    formattedValue = \`(\${value.slice(0, 3)}) \${value.slice(3)}\`;
                } else {
                    formattedValue = \`(\${value.slice(0, 3)}) \${value.slice(3, 6)}-\${value.slice(6, 10)}\`;
                }
            }
            
            input.value = formattedValue;
        }

        // Setup form validation
        function setupFormValidation() {
            const nameInput = document.getElementById('fullName');
            const phoneInput = document.getElementById('phone');
            const consentInput = document.getElementById('privacyConsent');
            const submitBtn = document.getElementById('submitBtn');
            
            function checkFormValidity() {
                const name = nameInput.value.trim();
                const phone = phoneInput.value.trim();
                const consent = consentInput.checked;
                const phoneRegex = /^\\(?([0-9]{3})\\)?[-. ]?([0-9]{3})[-. ]?([0-9]{4})$/;
                
                const isNameValid = name.length >= 2;
                const isPhoneValid = phoneRegex.test(phone.replace(/\\D/g, ''));
                
                if (consent) {
                    hideError('consentError');
                }
                submitBtn.disabled = !(isNameValid && isPhoneValid && consent);
            }
            
            nameInput.addEventListener('input', checkFormValidity);
            phoneInput.addEventListener('input', checkFormValidity);
            consentInput.addEventListener('change', checkFormValidity);
            
            // Initial check
            checkFormValidity();
        }

        function initializeModals() {
            const openButtons = document.querySelectorAll('[data-modal-open]');
            const closeButtons = document.querySelectorAll('[data-modal-close]');
            const modals = document.querySelectorAll('.modal');

            openButtons.forEach(button => {
                button.addEventListener('click', () => {
                    const modalId = button.getAttribute('data-modal-open');
                    const modal = document.getElementById(modalId);
                    if (modal) {
                        modal.classList.add('show');
                    }
                });
            });

            closeButtons.forEach(button => {
                button.addEventListener('click', () => {
                    const modal = button.closest('.modal');
                    if (modal) {
                        modal.classList.remove('show');
                    }
                });
            });

            modals.forEach(modal => {
                modal.addEventListener('click', event => {
                    if (event.target === modal) {
                        modal.classList.remove('show');
                    }
                });
            });

            document.addEventListener('keydown', event => {
                if (event.key === 'Escape') {
                    modals.forEach(modal => modal.classList.remove('show'));
                }
            });
        }

        // Show specific step
        function showStep(stepNumber) {
            // Hide all steps
            document.querySelectorAll('.step').forEach(step => {
                step.classList.remove('active');
            });
            
            // Show current step
            document.getElementById(\`step\${stepNumber}\`).classList.add('active');
            
            // Update progress
            updateProgress(stepNumber);
            
            // Update current step
            currentStep = stepNumber;
            
            // If showing thank you page, populate the information
            if (stepNumber === 2) {
                document.getElementById('submittedName').textContent = personalInfo.name;
                document.getElementById('submittedPhone').textContent = personalInfo.phone;
            }
        }

        // Update progress bar
        function updateProgress(step) {
            const progressFill = document.getElementById('progressFill');
            const progressText = document.getElementById('progressText');
            
            const percentage = (step / 2) * 100;
            progressFill.style.width = \`\${percentage}%\`;
            
            const progressMessages = {
                es: {
                    step1: 'Paso 1 de 2',
                    completed: 'Completado'
                },
                en: {
                    step1: 'Step 1 of 2',
                    completed: 'Completed'
                }
            };
            
            if (step === 1) {
                progressText.textContent = progressMessages[currentLanguage].step1;
            } else {
                progressText.textContent = progressMessages[currentLanguage].completed;
            }
        }

        // Initialize theme (default to light mode)
        function initializeTheme() {
            const savedTheme = localStorage.getItem('theme');
            
            // Default to light mode unless explicitly set to dark
            if (savedTheme === 'dark') {
                document.documentElement.setAttribute('data-theme', 'dark');
                updateThemeIcons(true);
            } else {
                document.documentElement.setAttribute('data-theme', 'light');
                updateThemeIcons(false);
            }
        }

        // Initialize language (default to Spanish)
        function initializeLanguage() {
            const savedLanguage = localStorage.getItem('language');
            currentLanguage = savedLanguage || 'es'; // Default to Spanish
            updateLanguageDisplay();
        }

        // Toggle language between Spanish and English
        function toggleLanguage() {
            currentLanguage = currentLanguage === 'es' ? 'en' : 'es';
            localStorage.setItem('language', currentLanguage);
            updateLanguageDisplay();
            updateProgress(currentStep); // Update progress text
        }

        // Update language display
        function updateLanguageDisplay() {
            const languageToggle = document.getElementById('languageToggle');
            const languageInput = document.getElementById('languageInput');
            // Show the opposite language (EN when in Spanish, ES when in English)
            languageToggle.textContent = currentLanguage === 'es' ? 'EN' : 'ES';
            if (languageInput) {
                languageInput.value = currentLanguage;
            }
            
            // Update all elements with data-es and data-en attributes
            document.querySelectorAll('[data-es][data-en]').forEach(element => {
                const text = element.getAttribute(\`data-\${currentLanguage}\`);
                if (text) {
                    element.textContent = text;
                }
            });
        }

        // Toggle dark mode
        function toggleDarkMode() {
            const currentTheme = document.documentElement.getAttribute('data-theme');
            const newTheme = currentTheme === 'dark' ? 'light' : 'dark';
            
            document.documentElement.setAttribute('data-theme', newTheme);
            localStorage.setItem('theme', newTheme);
            updateThemeIcons(newTheme === 'dark');
        }

        // Update theme icons
        function updateThemeIcons(isDark) {
            const sunIcon = document.querySelector('.sun-icon');
            const moonIcon = document.querySelector('.moon-icon');
            
            if (isDark) {
                // In dark mode, show sun icon (to switch to light)
                sunIcon.style.display = 'block';
                moonIcon.style.display = 'none';
            } else {
                // In light mode, show moon icon (to switch to dark)
                sunIcon.style.display = 'none';
                moonIcon.style.display = 'block';
            }
        }
    <\/script> </body> </html>`], ['<html lang="en" data-astro-cid-xb3inwvi> <head><meta charset="UTF-8"><meta name="viewport" content="width=device-width, initial-scale=1.0"><title>Form - Broker Lenders </title><link rel="icon" type="image/svg+xml" href="/logo.png">', `</head> <body data-astro-cid-xb3inwvi> <div class="container" data-astro-cid-xb3inwvi> <div class="header" data-astro-cid-xb3inwvi> <div class="controls" data-astro-cid-xb3inwvi> <button class="language-toggle" id="languageToggle" aria-label="Cambiar idioma" data-astro-cid-xb3inwvi>
EN
</button> <button class="dark-mode-toggle" id="darkModeToggle" aria-label="Alternar modo oscuro" data-astro-cid-xb3inwvi> <svg class="theme-icon sun-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" data-astro-cid-xb3inwvi> <circle cx="12" cy="12" r="5" data-astro-cid-xb3inwvi></circle> <path d="M12 1v2M12 21v2M4.22 4.22l1.42 1.42M18.36 18.36l1.42 1.42M1 12h2M21 12h2M4.22 19.78l1.42-1.42M18.36 5.64l1.42-1.42" data-astro-cid-xb3inwvi></path> </svg> <svg class="theme-icon moon-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" style="display: none;" data-astro-cid-xb3inwvi> <path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z" data-astro-cid-xb3inwvi></path> </svg> </button> </div> </div> <div class="form-container" data-astro-cid-xb3inwvi> <div class="progress-container" data-astro-cid-xb3inwvi> <div class="progress-bar" data-astro-cid-xb3inwvi> <div class="progress-fill" id="progressFill" data-astro-cid-xb3inwvi></div> </div> <div class="progress-text" id="progressText" data-astro-cid-xb3inwvi>Paso 1 de 2</div> </div> <img src="/logo.png" alt="Logo" class="logo" data-astro-cid-xb3inwvi> <!-- Step 1: Contact Information --> <div class="step active" id="step1" data-astro-cid-xb3inwvi> <h2 class="step-title" data-es="Informaci\xF3n de Contacto" data-en="Contact Information" data-astro-cid-xb3inwvi>Informaci\xF3n de Contacto</h2> <p class="step-description" data-es="Por favor proporciona tus datos de contacto para que podamos comunicarnos contigo." data-en="Please provide your contact details so we can get in touch with you." data-astro-cid-xb3inwvi>Por favor proporciona tus datos de contacto para que podamos comunicarnos contigo.</p> <form id="contactForm" action="/send-email.php" method="POST" data-send-email-url="https://brokerlenders.com/send-email.php" data-astro-cid-xb3inwvi> <div class="form-group" data-astro-cid-xb3inwvi> <label class="form-label" for="fullName" data-es="Nombre Completo *" data-en="Full Name *" data-astro-cid-xb3inwvi>Nombre Completo *</label> <input type="text" id="fullName" name="name" class="form-input" required data-astro-cid-xb3inwvi> <div class="error-message" id="nameError" data-es="Por favor ingresa tu nombre completo" data-en="Please enter your full name" data-astro-cid-xb3inwvi>Por favor ingresa tu nombre completo</div> </div> <div class="form-group" data-astro-cid-xb3inwvi> <label class="form-label" for="phone" data-es="N\xFAmero de Tel\xE9fono *" data-en="Phone Number *" data-astro-cid-xb3inwvi>N\xFAmero de Tel\xE9fono *</label> <input type="tel" id="phone" name="phone" class="form-input" placeholder="(555) 123-4567" required data-astro-cid-xb3inwvi> <div class="error-message" id="phoneError" data-es="Por favor ingresa un n\xFAmero de tel\xE9fono v\xE1lido" data-en="Please enter a valid phone number" data-astro-cid-xb3inwvi>Por favor ingresa un n\xFAmero de tel\xE9fono v\xE1lido</div> </div> <!-- Data Protection Notice --> <div class="data-protection-notice" data-astro-cid-xb3inwvi> <div class="protection-icon" data-astro-cid-xb3inwvi> <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" data-astro-cid-xb3inwvi> <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z" data-astro-cid-xb3inwvi></path> <path d="M9 12l2 2 4-4" data-astro-cid-xb3inwvi></path> </svg> </div> <p class="protection-text" data-es="\u{1F512} Tus datos est\xE1n protegidos y seguros. Utilizamos encriptaci\xF3n SSL y no compartimos tu informaci\xF3n con terceros." data-en="\u{1F512} Your data is protected and secure. We use SSL encryption and do not share your information with third parties." data-astro-cid-xb3inwvi>\u{1F512} Tus datos est\xE1n protegidos y seguros. Utilizamos encriptaci\xF3n SSL y no compartimos tu informaci\xF3n con terceros.</p> </div> <div class="consent-group" data-astro-cid-xb3inwvi> <label class="consent-item" data-astro-cid-xb3inwvi> <input id="privacyConsent" class="consent-input" type="checkbox" required data-astro-cid-xb3inwvi> <span class="consent-label" data-es="Acepto los" data-en="I accept the" data-astro-cid-xb3inwvi>Acepto los</span> <button class="consent-link" type="button" data-modal-open="termsModal" data-es="T\xE9rminos y Condiciones" data-en="Terms & Conditions" data-astro-cid-xb3inwvi>T\xE9rminos y Condiciones</button> <span class="consent-label" data-es="y la" data-en="and the" data-astro-cid-xb3inwvi>y la</span> <button class="consent-link" type="button" data-modal-open="privacyModal" data-es="Pol\xEDtica de Privacidad" data-en="Privacy Policy" data-astro-cid-xb3inwvi>Pol\xEDtica de Privacidad</button> </label> <label class="consent-item" data-astro-cid-xb3inwvi> <input id="updatesConsent" name="updatesConsent" class="consent-input" type="checkbox" checked data-astro-cid-xb3inwvi> <span class="consent-label" data-es="Quiero recibir novedades y actualizaciones por correo." data-en="I want to receive news and updates by email." data-astro-cid-xb3inwvi>Quiero recibir novedades y actualizaciones por correo.</span> </label> <div class="error-message" id="consentError" data-es="Debes aceptar los t\xE9rminos y la pol\xEDtica para continuar." data-en="You must accept the terms and privacy policy to continue." data-astro-cid-xb3inwvi>Debes aceptar los t\xE9rminos y la pol\xEDtica para continuar.</div> </div> <input type="hidden" id="languageInput" name="language" value="es" data-astro-cid-xb3inwvi> <button type="submit" class="btn btn-primary" id="submitBtn" data-astro-cid-xb3inwvi> <div class="loading-spinner" data-astro-cid-xb3inwvi></div> <span class="btn-text" data-es="Enviar" data-en="Submit" data-astro-cid-xb3inwvi>Enviar</span> </button> <!-- Success message after submit --> <div id="submitSuccess" class="success-message" aria-live="polite" data-astro-cid-xb3inwvi> <svg class="success-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" data-astro-cid-xb3inwvi> <polyline class="success-check" points="20,6 9,17 4,12" data-astro-cid-xb3inwvi></polyline> </svg> <span id="submitSuccessText" data-es="Enviado correctamente" data-en="Submitted successfully" data-astro-cid-xb3inwvi>Enviado correctamente</span> </div> <div id="submitError" class="submit-error" role="alert" aria-live="polite" data-astro-cid-xb3inwvi> <svg class="error-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" data-astro-cid-xb3inwvi> <circle cx="12" cy="12" r="9" data-astro-cid-xb3inwvi></circle> <path d="M12 7v6" data-astro-cid-xb3inwvi></path> <path d="M12 17h.01" data-astro-cid-xb3inwvi></path> </svg> <span id="submitErrorText" data-astro-cid-xb3inwvi></span> </div> </form> </div> <!-- Step 2: Thank You --> <div class="step" id="step2" data-astro-cid-xb3inwvi> <div class="thank-you" data-astro-cid-xb3inwvi> <div class="thank-you-icon" data-astro-cid-xb3inwvi> <svg class="checkmark" viewBox="0 0 24 24" data-astro-cid-xb3inwvi> <polyline points="20,6 9,17 4,12" data-astro-cid-xb3inwvi></polyline> </svg> </div> <h2 class="thank-you-title" data-es="\xA1Gracias!" data-en="Thank You!" data-astro-cid-xb3inwvi>\xA1Gracias!</h2> <p class="thank-you-message" data-es="Tu informaci\xF3n ha sido enviada exitosamente. Nos pondremos en contacto contigo pronto." data-en="Your information has been successfully submitted. We'll be in touch with you soon." data-astro-cid-xb3inwvi>Tu informaci\xF3n ha sido enviada exitosamente. Nos pondremos en contacto contigo pronto.</p> <div class="contact-info" data-astro-cid-xb3inwvi> <h3 data-es="Tu Informaci\xF3n" data-en="Your Information" data-astro-cid-xb3inwvi>Tu Informaci\xF3n</h3> <div class="contact-detail" data-astro-cid-xb3inwvi> <span class="contact-label" data-es="Nombre:" data-en="Name:" data-astro-cid-xb3inwvi>Nombre:</span> <span class="contact-value" id="submittedName" data-astro-cid-xb3inwvi>-</span> </div> <div class="contact-detail" data-astro-cid-xb3inwvi> <span class="contact-label" data-es="Tel\xE9fono:" data-en="Phone:" data-astro-cid-xb3inwvi>Tel\xE9fono:</span> <span class="contact-value" id="submittedPhone" data-astro-cid-xb3inwvi>-</span> </div> </div> </div> </div> </div> </div> <div id="privacyModal" class="modal" aria-hidden="true" data-astro-cid-xb3inwvi> <div class="modal-content" role="dialog" aria-modal="true" aria-labelledby="privacyTitle" data-astro-cid-xb3inwvi> <div class="modal-header" data-astro-cid-xb3inwvi> <h3 id="privacyTitle" class="modal-title" data-es="Pol\xEDtica de Privacidad" data-en="Privacy Policy" data-astro-cid-xb3inwvi>Pol\xEDtica de Privacidad</h3> <button class="modal-close" type="button" data-modal-close aria-label="Close" data-astro-cid-xb3inwvi>
\u2715
</button> </div> <div class="modal-body" data-astro-cid-xb3inwvi> <p data-es="Recopilamos tu nombre y tel\xE9fono para contactarte con informaci\xF3n sobre nuestros servicios. No compartimos tus datos con terceros sin tu consentimiento." data-en="We collect your name and phone number to contact you about our services. We do not share your data with third parties without your consent." data-astro-cid-xb3inwvi>Recopilamos tu nombre y tel\xE9fono para contactarte con informaci\xF3n sobre nuestros servicios. No compartimos tus datos con terceros sin tu consentimiento.</p> <p data-es="Puedes solicitar acceso, actualizaci\xF3n o eliminaci\xF3n de tus datos escribiendo a soporte." data-en="You can request access, updates, or deletion of your data by contacting support." data-astro-cid-xb3inwvi>Puedes solicitar acceso, actualizaci\xF3n o eliminaci\xF3n de tus datos escribiendo a soporte.</p> </div> </div> </div> <div id="termsModal" class="modal" aria-hidden="true" data-astro-cid-xb3inwvi> <div class="modal-content" role="dialog" aria-modal="true" aria-labelledby="termsTitle" data-astro-cid-xb3inwvi> <div class="modal-header" data-astro-cid-xb3inwvi> <h3 id="termsTitle" class="modal-title" data-es="T\xE9rminos y Condiciones" data-en="Terms & Conditions" data-astro-cid-xb3inwvi>T\xE9rminos y Condiciones</h3> <button class="modal-close" type="button" data-modal-close aria-label="Close" data-astro-cid-xb3inwvi>
\u2715
</button> </div> <div class="modal-body" data-astro-cid-xb3inwvi> <p data-es="La informaci\xF3n enviada ser\xE1 utilizada \xFAnicamente para fines de contacto y seguimiento sobre los servicios de Broker Lenders." data-en="The information submitted will be used only to contact you regarding Broker Lenders services." data-astro-cid-xb3inwvi>La informaci\xF3n enviada ser\xE1 utilizada \xFAnicamente para fines de contacto y seguimiento sobre los servicios de Broker Lenders   .</p> <p data-es="El env\xEDo del formulario no constituye una relaci\xF3n contractual hasta que exista una propuesta aceptada." data-en="Submitting this form does not create a contractual relationship until a proposal is accepted." data-astro-cid-xb3inwvi>El env\xEDo del formulario no constituye una relaci\xF3n contractual hasta que exista una propuesta aceptada.</p> </div> </div> </div> <script>
        // Global variables
        let currentStep = 1;
        let personalInfo = {
            name: '',
            phone: ''
        };
        let currentLanguage = 'es'; // Default to Spanish
        let sendEmailUrl = '/send-email.php';

        // Initialize when DOM is loaded
        document.addEventListener('DOMContentLoaded', function() {
            initializeTheme(); // Changed from initializeDarkMode to default to light
            initializeLanguage();
            setupFormValidation();
            showStep(1);
            initializeModals();
            
            // Setup event listeners
            const contactForm = document.getElementById('contactForm');
            if (contactForm?.dataset?.sendEmailUrl) {
                sendEmailUrl = contactForm.dataset.sendEmailUrl;
            }
            contactForm.addEventListener('submit', handleFormSubmit);
            document.getElementById('phone').addEventListener('input', formatPhoneNumber);
            document.querySelector('.dark-mode-toggle').addEventListener('click', toggleDarkMode);
            document.querySelector('.language-toggle').addEventListener('click', toggleLanguage);
        });

        // Handle form submission
        async function handleFormSubmit(e) {
            e.preventDefault();
            
            const name = document.getElementById('fullName').value.trim();
            const phone = document.getElementById('phone').value.trim();
            const consent = document.getElementById('privacyConsent').checked;
            const updates = document.getElementById('updatesConsent').checked;
            clearSubmitError();
            
            // Validate inputs
            if (!validateForm(name, phone, consent)) {
                return;
            }

            // Show loading state
            const submitBtn = document.getElementById('submitBtn');
            submitBtn.classList.add('loading');
            submitBtn.disabled = true;
            let successCompleted = false;
            
            try {
                const response = await fetch(\\\`\\\${sendEmailUrl}?t=\\\${Date.now()}\\\`, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    cache: 'no-store',
                    body: JSON.stringify({ name, phone, updatesConsent: updates, language: currentLanguage })
                });

                const responseText = await response.text();
                let responseData = {};
                try { responseData = JSON.parse(responseText); } catch {}
                const responseSuccess = response.ok && (responseData.success !== false);
                if (responseSuccess) {
                    // Store submitted data
                    personalInfo.name = name;
                    personalInfo.phone = phone;
                    
                    // Show success UI and keep button disabled/hidden
                    showSuccessUI();
                    successCompleted = true;
                    
                    // Transition after a short delay to thank-you step
                    setTimeout(() => {
                        showStep(2);
                    }, 1200);
                } else {
                    const isHtmlResponse = typeof responseText === 'string' && /<!doctype html|<html/i.test(responseText);
                    const apiMessages = {
                        es: isHtmlResponse
                            ? 'El servidor devolvi\xF3 HTML. Esto indica que send-email.php no se est\xE1 ejecutando como PHP.'
                            : 'No se pudo enviar en este momento. Revisa el servicio de correo.',
                        en: isHtmlResponse
                            ? 'The server returned HTML. This means send-email.php is not running as PHP.'
                            : 'We could not submit right now. Please check the email service.'
                    };
                    const detailText = responseData && (responseData.detail || responseData.message)
                        ? \\\` \\\${responseData.detail || responseData.message}\\\`
                        : responseText ? \\\` \\\${responseText.slice(0, 200)}\\\` : '';
                    showSubmitError(\\\`\\\${apiMessages[currentLanguage]}\\\${detailText}\\\`);
                }
            } catch (error) {
                console.error('Error submitting form:', error);
                const errorMessages = {
                    es: 'No se pudo conectar con el servicio de env\xEDo. Revisa tu conexi\xF3n.',
                    en: 'Could not reach the delivery service. Check your connection.'
                };
                showSubmitError(errorMessages[currentLanguage]);
            } finally {
                // Remove loading state only if not successful
                if (!successCompleted) {
                    submitBtn.classList.remove('loading');
                    submitBtn.disabled = false;
                }
            }
        }

        function showSuccessUI() {
            const submitBtn = document.getElementById('submitBtn');
            const successDiv = document.getElementById('submitSuccess');
            const successText = document.getElementById('submitSuccessText');
            clearSubmitError();
            const messages = { es: 'Enviado correctamente', en: 'Submitted successfully' };
            successText.textContent = messages[currentLanguage];
            submitBtn.classList.remove('loading');
            submitBtn.disabled = true;
            submitBtn.style.display = 'none';
            successDiv.classList.add('show');
        }

        function showSubmitError(message) {
            const submitError = document.getElementById('submitError');
            const submitErrorText = document.getElementById('submitErrorText');
            submitErrorText.textContent = message;
            submitError.classList.add('show');
        }

        function clearSubmitError() {
            const submitError = document.getElementById('submitError');
            const submitErrorText = document.getElementById('submitErrorText');
            submitErrorText.textContent = '';
            submitError.classList.remove('show');
        }

        // Validate form inputs
        function validateForm(name, phone, consent) {
            let isValid = true;
            
            const messages = {
                es: {
                    nameError: 'Por favor ingresa tu nombre completo',
                    phoneError: 'Por favor ingresa un n\xFAmero de tel\xE9fono v\xE1lido',
                    consentError: 'Debes aceptar los t\xE9rminos y la pol\xEDtica para continuar.'
                },
                en: {
                    nameError: 'Please enter your full name',
                    phoneError: 'Please enter a valid phone number',
                    consentError: 'You must accept the terms and privacy policy to continue.'
                }
            };
            
            // Validate name
            if (!name || name.length < 2) {
                showError('nameError', messages[currentLanguage].nameError);
                document.getElementById('fullName').classList.add('error');
                isValid = false;
            } else {
                hideError('nameError');
                document.getElementById('fullName').classList.remove('error');
            }
            
            // Validate phone
            const phoneRegex = /^\\\\(?([0-9]{3})\\\\)?[-. ]?([0-9]{3})[-. ]?([0-9]{4})$/;
            if (!phone || !phoneRegex.test(phone.replace(/\\\\D/g, ''))) {
                showError('phoneError', messages[currentLanguage].phoneError);
                document.getElementById('phone').classList.add('error');
                isValid = false;
            } else {
                hideError('phoneError');
                document.getElementById('phone').classList.remove('error');
            }

            if (!consent) {
                showError('consentError', messages[currentLanguage].consentError);
                isValid = false;
            } else {
                hideError('consentError');
            }
            
            return isValid;
        }

        // Show error message
        function showError(elementId, message) {
            const errorElement = document.getElementById(elementId);
            errorElement.textContent = message;
            errorElement.classList.add('show');
        }

        // Hide error message
        function hideError(elementId) {
            const errorElement = document.getElementById(elementId);
            errorElement.classList.remove('show');
        }

        // Format phone number as user types
        function formatPhoneNumber(e) {
            const input = e.target;
            const value = input.value.replace(/\\\\D/g, '');
            let formattedValue = '';
            
            if (value.length > 0) {
                if (value.length <= 3) {
                    formattedValue = \\\`(\\\${value}\\\`;
                } else if (value.length <= 6) {
                    formattedValue = \\\`(\\\${value.slice(0, 3)}) \\\${value.slice(3)}\\\`;
                } else {
                    formattedValue = \\\`(\\\${value.slice(0, 3)}) \\\${value.slice(3, 6)}-\\\${value.slice(6, 10)}\\\`;
                }
            }
            
            input.value = formattedValue;
        }

        // Setup form validation
        function setupFormValidation() {
            const nameInput = document.getElementById('fullName');
            const phoneInput = document.getElementById('phone');
            const consentInput = document.getElementById('privacyConsent');
            const submitBtn = document.getElementById('submitBtn');
            
            function checkFormValidity() {
                const name = nameInput.value.trim();
                const phone = phoneInput.value.trim();
                const consent = consentInput.checked;
                const phoneRegex = /^\\\\(?([0-9]{3})\\\\)?[-. ]?([0-9]{3})[-. ]?([0-9]{4})$/;
                
                const isNameValid = name.length >= 2;
                const isPhoneValid = phoneRegex.test(phone.replace(/\\\\D/g, ''));
                
                if (consent) {
                    hideError('consentError');
                }
                submitBtn.disabled = !(isNameValid && isPhoneValid && consent);
            }
            
            nameInput.addEventListener('input', checkFormValidity);
            phoneInput.addEventListener('input', checkFormValidity);
            consentInput.addEventListener('change', checkFormValidity);
            
            // Initial check
            checkFormValidity();
        }

        function initializeModals() {
            const openButtons = document.querySelectorAll('[data-modal-open]');
            const closeButtons = document.querySelectorAll('[data-modal-close]');
            const modals = document.querySelectorAll('.modal');

            openButtons.forEach(button => {
                button.addEventListener('click', () => {
                    const modalId = button.getAttribute('data-modal-open');
                    const modal = document.getElementById(modalId);
                    if (modal) {
                        modal.classList.add('show');
                    }
                });
            });

            closeButtons.forEach(button => {
                button.addEventListener('click', () => {
                    const modal = button.closest('.modal');
                    if (modal) {
                        modal.classList.remove('show');
                    }
                });
            });

            modals.forEach(modal => {
                modal.addEventListener('click', event => {
                    if (event.target === modal) {
                        modal.classList.remove('show');
                    }
                });
            });

            document.addEventListener('keydown', event => {
                if (event.key === 'Escape') {
                    modals.forEach(modal => modal.classList.remove('show'));
                }
            });
        }

        // Show specific step
        function showStep(stepNumber) {
            // Hide all steps
            document.querySelectorAll('.step').forEach(step => {
                step.classList.remove('active');
            });
            
            // Show current step
            document.getElementById(\\\`step\\\${stepNumber}\\\`).classList.add('active');
            
            // Update progress
            updateProgress(stepNumber);
            
            // Update current step
            currentStep = stepNumber;
            
            // If showing thank you page, populate the information
            if (stepNumber === 2) {
                document.getElementById('submittedName').textContent = personalInfo.name;
                document.getElementById('submittedPhone').textContent = personalInfo.phone;
            }
        }

        // Update progress bar
        function updateProgress(step) {
            const progressFill = document.getElementById('progressFill');
            const progressText = document.getElementById('progressText');
            
            const percentage = (step / 2) * 100;
            progressFill.style.width = \\\`\\\${percentage}%\\\`;
            
            const progressMessages = {
                es: {
                    step1: 'Paso 1 de 2',
                    completed: 'Completado'
                },
                en: {
                    step1: 'Step 1 of 2',
                    completed: 'Completed'
                }
            };
            
            if (step === 1) {
                progressText.textContent = progressMessages[currentLanguage].step1;
            } else {
                progressText.textContent = progressMessages[currentLanguage].completed;
            }
        }

        // Initialize theme (default to light mode)
        function initializeTheme() {
            const savedTheme = localStorage.getItem('theme');
            
            // Default to light mode unless explicitly set to dark
            if (savedTheme === 'dark') {
                document.documentElement.setAttribute('data-theme', 'dark');
                updateThemeIcons(true);
            } else {
                document.documentElement.setAttribute('data-theme', 'light');
                updateThemeIcons(false);
            }
        }

        // Initialize language (default to Spanish)
        function initializeLanguage() {
            const savedLanguage = localStorage.getItem('language');
            currentLanguage = savedLanguage || 'es'; // Default to Spanish
            updateLanguageDisplay();
        }

        // Toggle language between Spanish and English
        function toggleLanguage() {
            currentLanguage = currentLanguage === 'es' ? 'en' : 'es';
            localStorage.setItem('language', currentLanguage);
            updateLanguageDisplay();
            updateProgress(currentStep); // Update progress text
        }

        // Update language display
        function updateLanguageDisplay() {
            const languageToggle = document.getElementById('languageToggle');
            const languageInput = document.getElementById('languageInput');
            // Show the opposite language (EN when in Spanish, ES when in English)
            languageToggle.textContent = currentLanguage === 'es' ? 'EN' : 'ES';
            if (languageInput) {
                languageInput.value = currentLanguage;
            }
            
            // Update all elements with data-es and data-en attributes
            document.querySelectorAll('[data-es][data-en]').forEach(element => {
                const text = element.getAttribute(\\\`data-\\\${currentLanguage}\\\`);
                if (text) {
                    element.textContent = text;
                }
            });
        }

        // Toggle dark mode
        function toggleDarkMode() {
            const currentTheme = document.documentElement.getAttribute('data-theme');
            const newTheme = currentTheme === 'dark' ? 'light' : 'dark';
            
            document.documentElement.setAttribute('data-theme', newTheme);
            localStorage.setItem('theme', newTheme);
            updateThemeIcons(newTheme === 'dark');
        }

        // Update theme icons
        function updateThemeIcons(isDark) {
            const sunIcon = document.querySelector('.sun-icon');
            const moonIcon = document.querySelector('.moon-icon');
            
            if (isDark) {
                // In dark mode, show sun icon (to switch to light)
                sunIcon.style.display = 'block';
                moonIcon.style.display = 'none';
            } else {
                // In light mode, show moon icon (to switch to dark)
                sunIcon.style.display = 'none';
                moonIcon.style.display = 'block';
            }
        }
    <\/script> </body> </html>`])), renderHead());
}, "C:/Users/Admin/Documents/trae/broker-lenders/broker-lenders/src/pages/form.astro", void 0);

const $$file = "C:/Users/Admin/Documents/trae/broker-lenders/broker-lenders/src/pages/form.astro";
const $$url = "/form";

const _page = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
    __proto__: null,
    default: $$Form,
    file: $$file,
    url: $$url
}, Symbol.toStringTag, { value: 'Module' }));

const page = () => _page;

export { page };
